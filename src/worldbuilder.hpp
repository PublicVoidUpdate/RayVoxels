
vector<vector<int>> Chunks //pretty much just a set of bools, but i can store chunk ids if i want
{
    {0,0},
    {0,0}
};

void DrawChunk(Mesh chunkmesh[]) //this is gonna handle drawing the chunks properly, tile entities, and mobs.
{

} 